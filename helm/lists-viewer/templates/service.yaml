apiVersion: v1
kind: Service
metadata:
  name: {{ include "lists-viewer.fullname" . }}
  labels:
    {{- include "lists-viewer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lists-viewer.selectorLabels" . | nindent 4 }}
