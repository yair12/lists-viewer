{
  "name": "lists-viewer-e2e",
  "version": "1.0.0",
  "description": "End-to-end tests for Lists Viewer application",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "test:debug": "playwright test --debug",
    "test:offline": "playwright test tests/offline-workflows.spec.ts",
    "test:multi-tab": "playwright test tests/multi-tab-conflicts.spec.ts",
    "report": "playwright show-report",
    "codegen": "playwright codegen http://localhost:5173"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "@types/node": "^22.10.2"
  }
}
